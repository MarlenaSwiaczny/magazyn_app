// Test-cleanup-flow deprecated
// This script was used to validate the one-off cleanup CLI. The repository now
// removes uploads automatically when products are deleted, so this test script
// is left as a harmless placeholder and should not be necessary.

console.log('test-cleanup-flow is deprecated. Product deletion handles file cleanup.');
process.exit(0);
